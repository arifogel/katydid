# BUILD file for the vendored copy of nanoflann (a header-only nearest-neighbor search
# library), used by Katydid's Data module for k-d tree-based clustering.

load("@rules_cc//cc:cc_library.bzl", "cc_library")

package(default_visibility = ["//visibility:public"])

cc_library(
    name = "nanoflann",
    hdrs = ["nanoflann.hpp"],
    includes = ["."],
)
