# Drop this file at vendor/RapidXML/BUILD.bazel (after the External -> vendor rename).
# Header-only.

load("@rules_cc//cc:cc_library.bzl", "cc_library")

package(default_visibility = ["//visibility:public"])

cc_library(
    name = "rapidxml",
    hdrs = glob(["*.hpp"]),
    includes = ["."],
)
