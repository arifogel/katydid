load("@rules_cc//cc:cc_library.bzl", "cc_library")

# BUILD file for @rapidjson (github.com/miloyip/rapidjson), a header-only JSON library used by
# Scarab to read Katydid's JSON configuration files.

package(default_visibility = ["//visibility:public"])

cc_library(
    name = "rapidjson",
    hdrs = glob(["include/rapidjson/**/*.h"]),
    includes = ["include"],
)
